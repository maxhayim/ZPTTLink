# Core
pyserial
pynput
sounddevice
numpy
loguru
platformdirs

# macOS only
pyobjc; platform_system == "Darwin"

# Windows only
pycaw; platform_system == "Windows"

# Linux only (optional helpers for audio control)
pulsectl; platform_system == "Linux"
pyalsa; platform_system == "Linux"
